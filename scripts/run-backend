#!/usr/bin/env bash
set -euo pipefail

# Run the backend server on port 8080 (or custom port)
PORT="${1:-8080}"

echo "Starting backend server on port $PORT..."
./gradlew :backend:run --args="$PORT" --console=plain
