plugins {
    kotlin("jvm")
}

dependencies {
    // AWS CDK dependencies will be added in Milestone 3
    // Placeholder for now
}

java {
    toolchain {
        languageVersion.set(JavaLanguageVersion.of(21))
    }
}
